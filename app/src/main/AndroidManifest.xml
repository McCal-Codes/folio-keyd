<?xml version="1.0" encoding="utf-8"?>
<!-- Keyd asks for no permissions at all. A keyboard sees everything you type, so the only promise worth making
     is one the system enforces: with no INTERNET permission, nothing it sees can leave the phone. -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="${appLabel}"
        android:supportsRtl="true"
        android:theme="@android:style/Theme.DeviceDefault.DayNight">

        <service
            android:name=".KeysService"
            android:exported="true"
            android:label="${appLabel}"
            android:permission="android.permission.BIND_INPUT_METHOD">
            <intent-filter>
                <action android:name="android.view.InputMethod" />
            </intent-filter>
            <meta-data
                android:name="android.view.im"
                android:resource="@xml/method" />
        </service>

        <activity
            android:name=".SetupActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".SettingsActivity"
            android:label="@string/settings_title"
            android:exported="false" />
        <activity
            android:name=".ShortcutsActivity"
            android:label="@string/shortcuts_title"
            android:exported="false" />
    </application>
</manifest>
